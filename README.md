Simple react application
- webpack 
- es6/7 
- alt.js (flux)

install:
`npm install`

run dev server:
`npm run start`

build:
`npm run build`


todo:
- add tests
- replace alt.js with redux
- add immutablejs 
- use real api
